Part 1.

a.) They do have their own copies because when it creates a new process it copies the parent process variables into the child.

b.) There is not a fixed order because whichever process finishes first is going to display first.
    It is just a matter of which one executes before the other.
